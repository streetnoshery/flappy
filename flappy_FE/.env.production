# Option 2: Direct backend access (if backend is on different port/subdomain)
REACT_APP_API_URL=https://api.flappy.co.in

# Or if backend is on same server but different port
# REACT_APP_API_URL=https://flappy.co.in:3001